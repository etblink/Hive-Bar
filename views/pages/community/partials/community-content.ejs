<section class="community-shell" aria-labelledby="community-heading" data-m15-surface="community">
  <div class="app-content-frame">
    <header class="community-identity">
      <img
        src="/images/fourth-street-bar-logo.jpg"
        alt=""
        width="720"
        height="720"
        decoding="async"
        class="community-identity__logo max-w-full">
      <div class="community-identity__meta">
        <p class="app-kicker">Community</p>
        <h1 id="community-heading" class="app-page-title"><%= communityInfo.title %></h1>
        <p class="community-identity__handle"><%= communityInfo.name %> · 4th Street Bar on Hive</p>
      </div>
    </header>

    <div class="community-layout grid grid-cols-1 lg:grid-cols-2">
      <div class="community-main">
        <nav class="social-tabs" aria-label="Community content">
          <a
            href="/community"
            hx-get="/community/<%= communityName %>/community-posts?sort=created"
            hx-target="#community-feed"
            class="content-tab <%= activeView === 'posts' ? 'content-tab--active' : '' %>"
            <% if (activeView === 'posts') { %>aria-current="page"<% } %>>
            Community posts
          </a>
          <a
            href="/community/threads"
            hx-get="/community/threads"
            hx-target="#community-feed"
            class="content-tab <%= activeView === 'threads' ? 'content-tab--active' : '' %>"
            <% if (activeView === 'threads') { %>aria-current="page"<% } %>>
            Threads
          </a>
        </nav>

        <div id="community-feed" class="min-w-0" aria-live="polite">
          <% if (activeView === 'threads') { %>
            <%- include('community-thread-list', {
              container: threadsData.container,
              threads: threadsData.threads,
              profiles: threadsData.profiles,
              threadsContainerAccount
            }) %>
          <% } else if (feedError) { %>
            <section class="state-card mt-4" role="status">
              <h2>Posts are temporarily unavailable</h2>
              <p>The community itself is online, but its post feed could not be loaded. Nothing else on this page is affected.</p>
              <a href="/community" class="button-secondary mt-5">Try the feed again</a>
            </section>
          <% } else { %>
            <%- include('community-post-list', { postsPage, communityName }) %>
          <% } %>
        </div>
      </div>

      <aside class="community-aside" aria-label="Community information">
        <%- include('community-info-card') %>
      </aside>
    </div>
  </div>
</section>
